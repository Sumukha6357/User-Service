package com.jsp.springboot.user_service.exception;

public enum ErrorCode {
    USER_NOT_FOUND,
    USERNAME_ALREADY_EXISTS,
    INVALID_CREDENTIALS,
    ACCESS_DENIED,
    VALIDATION_FAILED,
    INTERNAL_ERROR
}
